<resources>
    <!-- Use Material Components DayNight; Compose will override colors at runtime. -->
    <style name="Theme.MobileCursorIDE" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>
